fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pytest==8.3.2
httpx==0.27.2

# Slice 7 LLM diagnosis tail. Imported lazily in app/llm_diagnosis.py
# (_default_transport only); the build/break tests inject a fake transport
# and never need it. Pin once installed.
anthropic
