.venv/
__pycache__/
*.pyc
.env
*.db
.pytest_cache/
data/

# Slice 12 Screen 2 - regenerated by scripts/make_trace_fixture.py, never
# hand-edited, never committed (also matched by *.db above; explicit for intent).
tests/fixtures/trace_demo.db

# Slice 12 demo DB - regenerated by scripts/make_demo_db.py from the corpus
# through the real pipeline, never hand-edited (also matched by data/ and *.db).
data/demo.db

# Slice 11 - run/freeze scratch. The committed artifacts are
# results/final_run.json and results/split_comparison_500.json ONLY.
results/*.meta.json
results/_verify_*.json
results/_*.db
results/*.db-journal
results/*.db-wal
results/*.db-shm
